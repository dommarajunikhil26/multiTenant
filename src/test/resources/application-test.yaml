spring:
  application:
    name: multitenant

  jpa:
    hibernate:
      ddl-auto: create-drop

  datasource:
    url: jdbc:postgresql://localhost:5433/multitenant
    username:
    password:

secret-key: ""
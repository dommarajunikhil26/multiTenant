spring:
  application:
    name: multitenant

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:postgresql://localhost:5433/multitenant
    username:
    password:

secret-key: ""